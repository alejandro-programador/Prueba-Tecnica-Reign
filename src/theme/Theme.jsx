import { createTheme } from "@mui/material";

const THEME = createTheme({
    palette: {
        primary: {
            main: '#00AEFF'
        },
        secondary: {
            main: '#E5E5E5',
            dark: '#676767'
        }
    }
})

export default THEME;